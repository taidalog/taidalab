{
  "name": "taidalab",
  "version": "1.0.0",
  "private": true,
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "dotnet fable watch src --run npx vite",
    "build": "dotnet fable src --run npx vite build",
    "build:testing": "dotnet fable src --define TESTING --run npx vite build --base /test.taidalab/"
  },
  "author": "taidalog",
  "license": "MIT",
  "devDependencies": {
    "vite": "^5.4.8"
  }
}
